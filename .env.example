# Environment variables for Docker Compose
# Copy this file to .env and update with your values
#
# Usage: cp .env.example .env

# ============================================
# Application Configuration
# ============================================

# Application Key (IMPORTANT: Change this for production!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
APP_KEY=HKrNVAer1r3jie7f7sOUSYZ5E3JTk9Ye

# ============================================
# Arduino RFID Serial Port Configuration
# ============================================

# Serial port where Arduino is connected
# Windows: COM3, COM4, COM5, etc.
# Linux: /dev/ttyUSB0, /dev/ttyACM0, etc.
# Mac: /dev/cu.usbserial-*, /dev/cu.usbmodem*
SERIAL_PORT=/dev/ttyUSB0
SERIAL_BAUD_RATE=9600

# ============================================
# PostgreSQL Database Configuration
# ============================================

# Database credentials (used by Docker Compose)
POSTGRES_USER=rfid_user
POSTGRES_PASSWORD=rfid_password
POSTGRES_DB=rfid_db

# Note: For production, use strong passwords!
# Example: POSTGRES_PASSWORD=$(openssl rand -base64 32)
